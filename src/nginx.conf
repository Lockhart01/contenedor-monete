server {
    listen 80;

    # Health check endpoint for ALB
    location /health {
        access_log off;
        add_header Content-Type text/plain;     # Keep logs clean from frequent ALB pings
        return 200 'healthy';
    }

    # Your application configuration...
}
